eureka:
  client:
    serviceUrl:
      defaultZone: http://${REGISTRY_HOST:localhost}:${REGISTRY_PORT:8761}/eureka/

database: mysql

spring:
  datasource:
    #    schema: "classpath:db/${database}/schema.sql"
    #    data: "classpath:db/${database}/data.sql"
    #    sql-script-encoding: UTF-8
    url: "jdbc:mysql://127.0.0.1:3306/myclouddb?characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=GMT%2B8&allowMultiQueries=true&allowPublicKeyRetrieval=true&useAffectedRows=true"
    username: "root"
    password: "123456"
    initialization-mode: always


logging:
  pattern:
    console: "%clr(%d{HH:mm:ss.SSS}){faint} %clr(${LOG_LEVEL_PATTERN:%5p}) %clr(-){faint} %clr([%t]){faint} %clr(%-40logger{39}){cyan}[%line]%clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:%wEx}"
  level:
    root: debug

server:
  port: ${PORT:8401}
